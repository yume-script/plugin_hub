# 🧩 플러그인 허브 (plugin_hub)

BookOasis에 설치된 카테고리 레벨 플러그인의 카테고리 뷰를 **하나의 화면에서 각 플러그인 이름의 탭으로 구분해 통합 표시**하는 플러그인입니다.

## 주요 기능

- 사이드바 [플러그인 허브] 진입 시, 표시하도록 선택한 플러그인들을 탭으로 통합 표시
- 모든 세션(일반/성인/오디오/비디오)에 노출되며, 각 보관함에서는 그 세션에 맞게 선택된 플러그인만 표시
- 설정 페이지에서 **카드형 UI**로 플러그인별 표시 세션 선택:
  - 카드마다 플러그인 이름 + 버전(VERSION 파일 파싱) + id 표시
  - 플러그인이 노출 가능한 세션(일반/성인/오디오/비디오)별 체크박스
  - 하나라도 켜면 해당 플러그인의 **개별 사이드바 탭은 자동 숨김** (전부 끄면 원복)
- 마지막으로 본 탭을 세션별로 기억해 재진입 시 자동 복원
- 개별 플러그인이 관리자 화면에서 **정지(비활성화)** 되면(`PLUGIN_ENABLED_<id>` = `0`), 해당 플러그인은 **탭 목록에서만 제외**됩니다. 설정 카탈로그(체크박스 카드)에는 계속 남아있고 **실제 저장된 선택값을 그대로 유지**한 채 체크박스 클릭만 잠깁니다(정지됨 배지 표시) — `save-config`가 설정 전체를 덮어쓰는 방식이라 값을 건드리면 저장 시 소실되므로, 값은 절대 바꾸지 않고 상호작용만 막습니다. 다시 활성화하면 예전 선택이 그대로 복원됩니다.
- **설정 바로가기**: 탭 바 우측 상단의 톱니바퀴(⚙) 아이콘을 누르면 사이드바 "환경설정" → "플러그인 설정" 탭 → 플러그인 허브 카드 아코디언까지 자동으로 이동/펼침 (실제 코어 클릭 경로를 그대로 재현: `#category-settings` 클릭 → `window.switchSettingsTab('plugins')` → 카드 헤더 클릭). 매번 하단까지 스크롤해서 찾을 필요 없음.
- **일괄 합치기/기본으로**: 카드 목록 위 "일괄 합치기" / "일괄 기본으로" 버튼으로 조작 가능한 셀렉트를 한 번에 "허브에 합치기" 또는 "기본"으로 바꿀 수 있습니다. 정지된 플러그인의 잠긴 셀렉트와 "완전히 숨기기"로 지정된 항목은 일괄 처리에서 제외되어 값이 보존됩니다.
- **제외 목록**: 설정 화면 하단 "목록에서 제외할 플러그인"에 콤마로 구분한 플러그인 id를 적으면 해당 id는 허브 카드/탭 후보에서 아예 빠집니다. 기본값(한 번도 저장한 적 없을 때)은 `bookoasis_plugins_viewer`(원본 "플러그인 모아보기") — 허브 안에 또 다른 통합 뷰어가 탭으로 들어가는 걸 방지합니다.

## 동작 원리

- **직접 마운트 방식**: 코어 `mountCategoryPluginUI`와 동일하게
  `innerHTML(css+html)` + `new Function('pluginId','container', js)` 로 각 뷰어를 탭 전환 마운트합니다.
  (iframe 격리 방식은 개별 뷰어가 코어 window 상태에 의존해 폐기)
- 탭 목록은 자체 `get_dashboard_data` 계약(`/api/media/dashboard/widgets/<id>/data`)으로 조회하고,
  각 뷰어의 UI 번들은 `/api/media/plugins/<id>/ui` 로 로드해 캐시합니다.
- 개별 뷰어는 `window.__bookOasisViewerCleanups` 레지스트리로 자체 정리되므로
  탭 전환 시 이전 뷰어의 리스너/옵저버 클린업이 보장됩니다. **기존 뷰어 코드는 0% 수정.**
- **사이드바 숨김**: 통합 표시로 선택된 플러그인은 런타임에 클래스 속성 `category_tab`을
  None으로 오버라이드해 코어 사이드바에서 제외합니다 (파일 무수정, 메모리상 오버라이드,
  선택 해제·재시작 시 원복). 데이터 API는 계속 동작합니다.

## 설정 저장 구조

- 설정 키: `SHOW_<plugin_id>__<session>` (예: `SHOW_bookoasis_11t__adult`) — 불리언
- 저장 위치: general DB `PLUGIN_CONFIG_plugin_hub` (코어 설정 저장 계약 그대로 사용)
- 기본값: 전부 꺼짐 = 기존처럼 개별 탭 표시

## 버전별 변경 이력

- **v2.6.0** — 코드 리뷰 반영: (1) `_apply_session_overrides`의 `force_refresh_config` 인자가 실제로는 아무 것도 하지 않던 죽은 파라미터였던 것을 고쳐, `_load_general_config`/`_is_plugin_enabled`에 짧은(3초) TTL 캐시를 실제로 연결 — 사이드바에 플러그인이 N개 있을 때 렌더링 1회당 DB 쿼리가 N에 비례해 반복되던 성능 문제 개선(저장 직후 조회 시점에는 여전히 캐시를 건너뛰고 최신값을 읽음). (2) `script.js`의 사이드바 탭 숨김/복원 로직을 마커 속성(`data-plugin-hub-hidden`) 기반으로 변경 — 이전에는 페이지 전체에서 `[data-plugin-id]`/`[data-tab-id]`를 가진 요소를 광범위하게 훑어 복원 여부를 판단했는데, 이러면 허브가 직접 숨기지 않은(다른 이유로 숨겨진) 요소까지 실수로 복원할 위험이 있었음. 이제 허브가 직접 숨긴 요소만 추적해 되돌린다. (3) 설정 변경 시(`plugin_hub:config_updated`) `script.js`의 `bundleCache`도 함께 비우도록 수정 — 병합된 플러그인이 `update_manifest`로 자기 UI 파일을 자동 업데이트해도 새로고침 전까지 옛 번들이 계속 캐시되어 보이던 문제 해결. (4) 위 "일괄 선택/해제" 문서 표현을 실제 버튼 라벨("일괄 합치기"/"일괄 기본으로")에 맞게 수정. (5) 버전 파싱(`_read_plugin_version`)이 "플러그인 폴더명 == plugin id" 관례에 의존한다는 점과, 정지 판정(`_is_plugin_enabled`)이 세션 구분 없이 항상 general DB만 조회한다는 전제를 코드 주석으로 명시(동작 변경은 없음, 코어 구현과의 교차 확인 필요 지점으로 남겨둠).
- **v1.6.0** — 개별 사이드바 탭 숨김 기능 실사용 검증 완료 (디버그 로그로 실제 요청까지 추적해 확인): `/api/media/category-plugins?type=adult` 요청에서 백엔드가 정확히 숨김 판정을 내리는 것 확인. 디버그 로그 제거하고 예외 시에만 남는 조용한 에러 로그로 정리, `_discover_viewer_classes()`의 플러그인별 처리를 개별 try/except로 분리(한 플러그인 처리 실패가 이후 플러그인 전체를 건너뛰게 만들던 구조적 위험 제거).
- **v1.5.0** — 카드 목록 위에 "일괄 선택" / "일괄 해제" 버튼 추가 (정지된 플러그인의 잠긴 체크박스는 제외하고 처리). **아쿠니스님 요청사항입니다.**
- **v1.4.2** — 탭 바 톱니바퀴(설정 바로가기) 버튼이 의도대로 동작하지 않아 우선 숨김 처리 (코드는 유지).
- **v1.4.1** — 설정 바로가기 버튼의 이동 로직 보강: `settings-tab-plugins`가 페이지 로드 시부터 항상 DOM에 존재해 대기 조건으로 못 쓴다는 점을 반영해, 실제 탭 버튼(`.settings-tab-btn`)과 `window.switchSettingsTab`을 함께 폴링하고 디버그 로그 추가.
- **v1.4.0** — 탭 바 우측 상단에 톱니바퀴(⚙) 버튼 추가: 사이드바 "환경설정" → "플러그인 설정" 탭 → 허브 카드 아코디언까지 자동 이동/펼침 시도.
- **v1.3.0** — "목록에서 제외할 플러그인" 설정 추가 (`EXCLUDED_IDS`, 기본값 `bookoasis_plugins_viewer`) — 허브 안에 원본 "플러그인 모아보기" 같은 또 다른 통합 뷰어가 들어가는 것을 방지.
- **v1.2.0** — 정지된 플러그인의 체크박스 값을 강제로 끄지 않고 **실제 저장값을 그대로 유지**한 채 클릭만 잠그도록 변경. (`save-config`가 설정 전체를 덮어쓰는 방식이라, 이전 방식은 저장할 때마다 원래 선택값이 사라지는 버그가 있었음.) 탭 순서 레인에는 실제로 지금 뜨는 탭만 표시.
- **v1.1.1** — 설정 카탈로그(`config_schema`) 생성 로직에 있던 `NameError` 런타임 버그 수정 (세션 순회 루프가 실수로 삭제되어 있었음). 이 버그 때문에 플러그인 관리 목록에서 plugin_hub 자체가 조용히 빠지고 있었음. 이번엔 실제로 코드를 실행해 검증.
- **v1.1.0** — 정지된 플러그인을 설정 카탈로그에서 완전히 빼지 않고 계속 표시하도록 변경 (탭에서만 제외).
- **v1.0.4** — `PluginDatabaseGateway.get_setting()`이 값을 찾으면 `{"value": ...}` 형태로 감싸서 반환한다는 걸 반영해 언랩 처리 수정 (정지 판별이 항상 실패하던 버그).
- **v1.0.3** — 정지 판별 기본값 로직 수정: 값이 없거나 `'1'`이면 활성, 명시적으로 `'0'`일 때만 비활성으로 판정하도록 코어(`metadata_factory.py`)와 동일하게 맞춤.
- **v1.0.2** — 정지된 플러그인을 탭 목록에서 제외하는 기능 최초 추가 (이후 버전에서 세부 로직 보정).
- **v1.0.1** — 원본 플러그인의 실제 프론트엔드 코드(`script.js`/`settings.js`)를 확보해 이식: `/api/media/plugins/<id>/ui` 응답이 `{success, bundle: {css,html,js}}` 구조라는 점, 현재 세션 판별은 URL 쿼리가 아니라 `document.documentElement.getAttribute('data-library-type')`라는 점, `settings.js`는 코어가 `new Function('window','pluginId','root','config', js)`로 실행하는 함수 본문이라는 점을 반영해 전면 수정.
- **v1.0.0** — 최초 작성. `madnite1/bookoasis_plugins_viewer`("플러그인 모아보기")와 동일한 기능(여러 카테고리 뷰 플러그인을 탭으로 통합 표시)을 하는 신규 플러그인으로 시작.

## 파일 구조

```
plugin_hub/
  __init__.py      # 제공자 노출
  plugin_hub.py     # 제공자 + 동적 config/카탈로그 + category_tab 오버라이드
  index.html        # 탭 바 + 패널 컨테이너
  style.css         # 테마 CSS 변수 연동 탭 스타일
  script.js         # 탭 목록 조회 + 뷰어 직접 마운트/클린업
  settings.html     # 설정 페이지 카드형 UI 골격
  settings.css      # 카드 그리드 스타일
  settings.js       # 카탈로그 fetch + 카드 렌더 (저장은 코어 폼 submit 이용)
  VERSION           # 버전 파일
```

## 주의 / 설계 메모

- 탭·카드 목록은 팩토리 디스커버리 기반 동적 생성 — 뷰어 설치/삭제 시 자동 반영 (자기 자신 제외).
- settings.js의 체크박스 `name`이 설정 키와 동일하므로 코어의 "설정 저장" 버튼이
  그대로 값을 수집·저장합니다 (별도 저장 API 불필요).
- gunicorn 1워커 전제: category_tab 런타임 오버라이드는 프로세스 메모리 기준입니다.
- `.py` 변경은 컨테이너 재시작 필요, html/css/js는 bind mount로 즉시 반영.
